# Used by docker-compose.yml at the root. Copy to .env and adjust.
# Per-service local dev still uses server/.env.example and client/.env.example.

JWT_SECRET=dev-secret-change-me
JWT_EXPIRES_IN=7d
CORS_ORIGIN=http://localhost:8090
VITE_API_URL=http://localhost:4000/api

MONGO_PORT=27018
API_PORT=4000
CLIENT_PORT=8090
